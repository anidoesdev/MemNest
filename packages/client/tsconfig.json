{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The client runs in browsers and Node alike: only the web platform (fetch, streams) in src.
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["src", "test"]
}
